<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for Night mode -->
    <style name="Theme.WhatsForDinner" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Dark mode colors -->
        <item name="colorPrimary">@color/night_primaryVariant</item>
        <item name="colorPrimaryVariant">@color/night_primary</item>
        <item name="colorSecondary">@color/night_secondary</item>

        <!-- Arka plan gradyanı -->
        <item name="android:windowBackground">@drawable/background_gradient</item>

        <!-- Text ve ikon renkleri -->
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorOnBackground">@color/black</item>
        <item name="colorOnSurface">@color/black</item>

        <!-- Surface renkleri -->
        <item name="colorSurface">@color/black</item>
        <item name="colorControlNormal">@color/black</item>

    </style>
</resources>
